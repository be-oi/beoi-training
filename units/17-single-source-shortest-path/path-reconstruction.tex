\frame{
    \frametitle{Path reconstruction}
    \framesubtitle{Breath First Search}
    
    \begin{itemize}[<+->]
        \item For all these algorithms\\
        \item Keep mapping $parent$
        \item When expanding/relaxing to node $v$:
        \item $parent[v]$ = the node from where you came
        \item Goal found $\Rightarrow$ move up the \emph{parent chain} until the starting node
    \end{itemize}
}
